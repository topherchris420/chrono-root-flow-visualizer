import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const MissionPlanning = () => {
  return (
    <Card className="w-full h-full bg-card/95 backdrop-blur-sm border-border/50 shadow-xl">
      <CardHeader>
        <CardTitle>Mission Planning</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Mission planning interface will be here.</p>
      </CardContent>
    </Card>
  );
};
